"use client"

import type { WorkspaceProps }  from "@/types/workspace"

const MobileWorkspace = ({
   openedApps,
		openApp,
		closeApp,
		hideApp,
}: WorkspaceProps) => {

	return (
		<div>
			Mobile
		</div>
	)
}

export default MobileWorkspace
